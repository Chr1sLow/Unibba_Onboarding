import styled from 'styled-components';

export const GameListComponentWrapper = styled.div`
    display: flex;
    flex-direction: column;
    gap: 16px;
`;

export const EmptyState = styled.div`
    font-size: 24px;
    margin-top: 32px;
`;
