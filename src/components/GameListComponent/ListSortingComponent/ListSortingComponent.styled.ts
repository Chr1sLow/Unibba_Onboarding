import styled from 'styled-components';

export const ListSortingComponentWrapper = styled.div`
    display: flex;
    justify-content: flex-start;
    padding: 16px;
`;

export const Sorter = styled.select`
    margin-left: 4px;
`